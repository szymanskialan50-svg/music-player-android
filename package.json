{
  "name": "music-player-ios",
  "version": "1.0.0",
  "description": "Music Player Wrapper",
  "main": "index.js",
  "scripts": {
    "build": "npx cap sync android",
    "build:android": "npx cap sync android",
    "build:ios": "npx cap sync ios"
  },
  "dependencies": {
    "@capacitor/android": "^6.0.0",
    "@capacitor/cli": "^6.0.0",
    "@capacitor/core": "^6.0.0",
    "@capacitor/ios": "^6.0.0"
  },
  "devDependencies": {
    "@capacitor/assets": "^3.0.5",
    "typescript": "^5.0.0"
  }
}
